Place the login background image here and name it `ispace-bg.png`.

You can copy the graphic supplied by the design team (shown in the conversation) into this folder. The `AuthLayout` component imports it as:

```js
import loginBg from '../../assets/ispace-bg.png';
```

Tailwind's `bg-cover bg-center` utility will handle sizing.
